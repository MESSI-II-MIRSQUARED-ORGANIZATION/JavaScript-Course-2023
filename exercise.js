// 1. Write a JavaScript for loop that iterates from 0 to 15. For each iteration, it checks if the current number is odd or even, and displays a message on the screen.
// Sample Output :
// "0 is even"
// "1 is odd"
// "2 is even"
for (let K = 0; K <=15; K++) {
    if ( K % 2 ===0) {
        console.log(`${K} is even`); 
    } else{
        console.log(`${K} is odd`);
    }
    
}



//2. Write a JavaScript program that displays the largest integer among two integers.
let a = 40;
let b = 100;
if (a>b) {
    console.log(`${a} is greater`);
} else {
    console.log(`${b} is greater`);
}